# Roadmap di miglioramento — UniFi Protect Monitor

> Versione analizzata: `project/main.js` (232 righe), `project/package.json`, Electron 28.3.3, NSIS per-machine.
> Riferimento di confronto: `digital195/unifi-protect-viewer` (MIT), sorgenti + build 2.4.0.
> I numeri di riga citati si riferiscono al file `main.js` attuale (232 righe), non allo snapshot in `project/_original_1.6.4/`.

---

## 1. Bug reali da correggere subito

| id | problema | fix | impatto |
|---|---|---|---|
| `implement-or-remove-auto-reboot` | `autoReboot` (main.js:30) è dichiarato e **mai letto**; `oraReboot` (:31) è renderizzato in "Refresh ore:" (:153) e salvato (:199) ma nessun timer esiste (`grep setInterval` = 0). L'operatore configura 03:00, legge "Salvato!" e non succede nulla. | Ticker da 60 s dopo `createWindows()`: confronto `HH:MM` con guardia `lastRefreshDay` (no ri-fire nel minuto, resistente al DST/sleep); chiama `loadVista(vistaAttiva)` — **non** `viste[1]`. Aggiungere la checkbox `autoReboot` accanto a `#fs` (:152) e `config.autoReboot = !!d.autoReboot` nel payload di `saveAll()` (:184) e in `save-all-data` (:199). Ri-armare il timer dentro `save-all-data` o l'orario nuovo non si applica fino al riavvio. Default `false` per non introdurre un evento notturno sugli impianti esistenti. | Alto |
| `corrupt-config-quarantine` | main.js:42-44 `try { config = Object.assign({}, defaultConfig, JSON.parse(...)) } catch (e) {}` — catch **vuoto**. Un file troncato riporta silenziosamente le 10 viste a `https://unifi.ui.com/` (:38) e `passwordApp` al default di fabbrica `'Uat07Iot'` (:28), confrontato a :135 e :194. Il file corrotto è anche l'unica copia e viene sovrascritto al primo salvataggio. | Nel catch: `fs.renameSync(configPath, configPath + '.corrupt-' + Date.now())` + flag; mostrare `dialog.showErrorBox` **dentro** `app.whenReady()` (il load è a livello di modulo, `showErrorBox` prima di whenReady è inaffidabile su Windows). Mai bloccare l'avvio: la parete deve comunque mostrare qualcosa. | Alto |
| `atomic-config-write` | Quattro `fs.writeFileSync` non atomici e per lo più non protetti: :89 (export), :97 (import), :196 (cambio password), :199 (save-all-data). Un blackout a metà scrittura tronca il JSON e innesca il punto precedente. Inoltre :199 mostra "Salvato!" **incondizionatamente**, anche se la write ha lanciato. | Un solo `saveConfig()`: `writeFileSync(configPath+'.tmp', JSON.stringify(config, null, 2))` → `fsyncSync` → `renameSync` (atomico su NTFS), il tutto in try/catch che ritorna booleano. Le tre write interne passano da lì; il dialog "Salvato!" (:199) diventa condizionato al ritorno. Fallback a write diretta nel catch: `renameSync` su Windows dà EPERM se antivirus/backup tengono l'handle. Proteggere anche la export (:89) — la destinazione è spesso una chiavetta non scrivibile. | Alto |
| `shortcut-ignores-attiva` | `loadVista()` (:79-84) controlla `!v || !v.url` ma **mai** `attiva`, mentre il menu contestuale filtra (`:217 if (config.viste[i] && config.viste[i].attiva)`). Su installazione fresca le viste 6-9 sono `attiva:false` su `https://unifi.ui.com/` (:38): un Ctrl+7 accidentale butta la parete sulla pagina di login cloud UniFi. | Guardia **nel loop di registrazione** (:224), non dentro `loadVista` (:209 `loadVista(1)` e :112/:219 post-logout devono continuare a funzionare): `if (!config.viste[i] || !config.viste[i].attiva) return;` + no-op `if (i === vistaAttiva) return;` (oggi un Ctrl+1 ripetuto forza un reload completo e qualche secondo di nero). Breaking: chi usa un hotkey su una vista deselezionata lo perde — citarlo nel changelog. | Medio |
| `splash-fixed-2s-timeout` | :210 `setTimeout(() => { ...; win.show(); win.focus(); }, 2000)` non ha guardia. Chiudendo l'app entro quei 2 s, `win.on('close')` (:213) → `beginShutdown()` → `win.destroy()` (:75): il timer chiama `show()` su una finestra distrutta e lancia nel main process. Inoltre :208 ha `show:false` senza `backgroundColor` → flash bianco su parete scura. | `revealMainWindow()` idempotente con latch `revealed`, guardie `isDestroyed()` su `splash` e `win`, `splash.destroy()` invece di `close()`, `win.once('ready-to-show', reveal)` + timeout di sicurezza a 15-20 s, e `backgroundColor: '#1c2b39'` a :208 (stesso tono di `wallpaper.html`). Attenzione: con `logoutOnStart` la finestra nasce dopo `logoutUnifi()` (budget `withTimeout` ~15 s a :66-68), quindi il cap non può essere corto. | Medio |
| `modal-prompt-auto-dismiss` | `promptWin` è una `let` locale a :117 e `settingsWin` a :139: nessuna guardia di re-entranza. Ogni F10 (:225) impila un'altra modale. `ipcMain.removeAllListeners('pass-ok'); ipcMain.once('pass-ok', cb)` (:131-132) pulisce solo alla **chiamata successiva**, quindi un prompt abbandonato lascia un listener armato. | Portare `promptWin`/`settingsWin` a scope di modulo; `if (x && !x.isDestroyed()) { x.focus(); return; }`; `x.on('closed', () => { x = null; ipcMain.removeAllListeners('pass-ok'); })`. Non serve l'auto-dismiss a 60 s: entrambe le finestre hanno già un bottone di chiusura (`window.close()` a :127 e :180). | Basso |
| `password-prompt-keyboard` | :123 `<input type="password" id="pass" ... autofocus>` senza `onkeydown`: Enter e Escape non fanno nulla, l'unico submit è l'`onclick` a :126. Su ogni singolo accesso alle impostazioni serve il mouse. | `onkeydown="if(event.key==='Enter'){submit()} if(event.key==='Escape'){window.close()}"` e fattorizzare l'inline handler in un `submit()` condiviso. Verificare che Escape chiuda davvero una `modal:true, frame:false`. | Basso |
| `settings-window-fits-workarea` | :139 `new BrowserWindow({ width: 850, height: 1050, parent: win, modal: true })` senza `x/y`, senza `min/max`, senza uso di `screen`. Su pannello 1366×768 o 1024×768 finiscono fuori schermo, in quest'ordine: Chiudi + versione (:180), blocco "Modifica Password" (:171-179), riga Backup/Restore/Svuota Cache. Il body (:141) non ha `overflow-y`, quindi nemmeno massimizzando si raggiungono. | Clamp con `screen.getDisplayNearestPoint(screen.getCursorScreenPoint()).workAreaSize` (non `getPrimaryDisplay`: la parete è spesso il monitor secondario) + `minWidth:700, minHeight:400`; body a `height:100vh; box-sizing:border-box; overflow-y:auto`; lista viste da `max-height:350px` (:143) a `max-height:40vh`. Le due metà devono uscire insieme, altrimenti il taglio si sposta soltanto dentro la finestra. | Medio |

---

## 2. Sicurezza

| id | problema | fix | impatto |
|---|---|---|---|
| `escape-config-in-settings-html` | **Il difetto più grave del file.** :147 `value="${config.viste[num].nome}"` e :148 idem per `.url`, interpolati senza escape in un documento caricato a :190 come `data:text/html` in una finestra creata a :139 con `nodeIntegration: true, contextIsolation: false`. `encodeURIComponent` è codifica di trasporto: il browser decodifica `%22` in `"` prima del parsing. Vettore reale: `import-config` (:92-101) legge un JSON scelto dall'operatore, `Object.assign` senza validazione, scrive e rilancia. Un file di Backup preparato ad arte = esecuzione di codice con Node completo. | **Hotfix da 4 righe, da fare per primo e indipendentemente dal resto**: un `esc()` che sostituisce `& < > " '` applicato a **ogni** `${}` delle due pagine — incluso `config.oraReboot` a :153 e la stringa di versione, altrimenti la patch è parziale. Nella `import-config` aggiungere `new URL()` con protocollo http/https e cap di lunghezza, riusando il `dialog.showErrorBox("Errore", "File non valido")` già presente a :99. Nota: le label del menu contestuale (:217-218) passano da `Menu.buildFromTemplate` e **non** sono un vettore. | Alto |
| `settings-window-data-over-ipc` | Struttura che rende possibile il punto precedente: `promptWin` (:117) e `settingsWin` (:139) hanno entrambe `nodeIntegration:true, contextIsolation:false`, caricano `data:` URL (:130, :190) e contengono `const { ipcRenderer } = require('electron')` (:128, :182) con handler inline `onclick="ipcRenderer.send(...)"` (:126, :162, :166-168). Origine opaca, nessuna CSP possibile, nessun test possibile. | Spedire `settings.html` + `password.html` + `preload-settings.js` reali; finestre con `preload`, `contextIsolation: true`, `nodeIntegration: false`, `loadFile(path.join(__dirname,'settings.html'))`. Bridge **nominale** via `contextBridge`, mai un forwarder generico: `{ getConfig, saveConfig, changePassword, exportConfig, importConfig, clearCache, logoutNow, submitPassword, close }`. La pagina riempie le 10 righe con `input.value = vista.nome` (assegnazione di proprietà DOM, immune per costruzione). CSP `default-src 'none'; script-src 'self'` nel file. `cfg:get` deve restituire un oggetto `{nome,url,attiva}` ricostruito, **non** il riferimento vivo a `config`. **Trappola di packaging**: `package.json:20-29` è una whitelist esplicita di 8 file — se i nuovi file non ci entrano, `npm start` funziona e la build NSIS apre una finestra impostazioni vuota, scoperta solo presso il cliente. Canali da riportare: 7 (`export-config`, `import-config`, `clear-cache`, `logout-now`, `req-p`, `save-all-data`, `check-pass-val`/`pass-result`). Lasciare `sandbox:true` fuori dal primo giro. | Alto |
| `strip-default-menu-devtools` | `grep setApplicationMenu main.js` = 0. `Menu` è importato a :1 e usato solo a :221 per il popup. `autoHideMenuBar: true` (:139, :208) nasconde la barra ma **non** de-registra gli acceleratori: Alt la rivela, Ctrl+Shift+I apre DevTools. Su `promptWin` (nodeIntegration, pre-autenticazione) DevTools è una REPL Node: `require('fs').readFileSync(configPath,'utf8')` stampa la password in chiaro — il gate password è aggirabile dal gate stesso. | `Menu.setApplicationMenu(null)` a livello di modulo prima di `app.whenReady` — una riga, elimina barra, Ctrl+W, Ctrl+R, Ctrl+Shift+I e l'F11 nativo. In più, `app.on('web-contents-created')` con `before-input-event` che blocca Ctrl+Shift+I / F12 / Ctrl+R / Ctrl+W **solo se `app.isPackaged`**. Obbligatorio nello stesso commit: aggiungere una voce `🔄 Ricarica` al template a :216-222 → `loadVista(vistaAttiva)`, altrimenti si toglie al tecnico l'unico modo di sbloccare una pagina freezata senza mouse. | Alto |
| `scrypt-password-hash` | `passwordApp: 'Uat07Iot'` (:28) è identica su **ogni** installazione, in chiaro dentro un asar che `npx asar extract` apre in un comando; persistita in chiaro (:196, :199); confrontata con `===` (:135, :194); e copiata testualmente in **ogni** file di Backup (:89 `JSON.stringify(config, null, 2)`) che viaggia su chiavetta e per email. | `node:crypto`, nessuna dipendenza. Mantenere il nome chiave `passwordApp` come stringa che accetta **sia** il vecchio plaintext (confronto `===`, poi re-hash e riscrittura una tantum) **sia** il nuovo formato `salt:hash` (scrypt N=16384 r=8 p=1, `timingSafeEqual`). Entrambi i confronti (:135 e :194) vanno cambiati nello **stesso** commit. Escludere/ignorare salt e hash in export (:89) e import (:96). Documentare il reset: cancellare `%APPDATA%\unifi-protect-monitor\viste_config.json`. La migrazione deve reggere il path di parse fallito a :43 — oggi un file corrotto ripristina silenziosamente la password di fabbrica. **Nota di rollback**: dopo la migrazione la build 1.6.4 installata altrove non riesce più a confrontare e perde impostazioni *e* uscita "❌ Chiudi". | Alto |
| `validated-config-import` | `import-config` (:92-101) fa `Object.assign({}, defaultConfig, JSON.parse(...))`: la `passwordApp` del file importato **diventa** la password delle impostazioni, senza che quella corrente venga mai richiesta. Poi scrive e rilancia immediatamente (:98 `app.relaunch(); app.exit();`), senza anteprima. | Tre righe dentro il try esistente: `parsed.passwordApp = config.passwordApp` (una credenziale non si importa mai), un controllo di **forma** minimo (10 slot, `url`/`nome` stringhe), e un dialog di conferma che dica cosa sta per essere sostituito. Validare la forma, non i contenuti: un import troppo severo rifiuta i backup vecchi del cliente proprio quando serve. Lato export (:89) omettere il campo password e annotarlo nel file. *(Refutato: il "lockup da slot mancanti" non può accadere — :46 riempie ogni buco e `loadVista` esce su `!v || !v.url`.)* | Medio |
| `navigation-allowlist` / `block-popup-windows` | `grep setWindowOpenHandler` e `will-navigate` = 0; l'unico listener su `win.webContents` è `context-menu` (:215). Un `window.open()` o `target=_blank` dalla pagina Protect crea una BrowserWindow che **non è `win`**: fuori dai Ctrl+0..9, fuori dal menu contestuale, fuori dal toggle fullscreen — su parete fullscreen senza mouse è indismissibile. Aggravante: :6 `ignore-certificate-errors` è globale, quindi vale anche per quel popup verso qualsiasi host. | `win.webContents.setWindowOpenHandler(({url}) => { if (sameOrigin(url, config.viste[vistaAttiva].url)) loadVista-style loadURL nella stessa finestra; return {action:'deny'}; })` — sempre deny della finestra separata. `sameOrigin` con `new URL(a).origin === new URL(b).origin` in try/catch, **non** lo `startsWith` del riferimento (che lascerebbe passare `https://unifi.ui.com.evil.tld`). Su `will-navigate` bloccare **solo** gli schemi non-`http(s)` (`file:`, `data:`, `javascript:`): una allowlist per hostname romperebbe il login SSO, che rimbalza su host `ui.com` diversi da quelli configurati. Loggare ogni deny nella prima release: la vista 0 "Registrazioni" potrebbe usare `window.open` per l'export clip. | Medio |
| `scoped-certificate-trust` | :6 `app.commandLine.appendSwitch('ignore-certificate-errors')` disattiva la validazione TLS **process-wide**, prima che esista qualsiasi finestra, con le 10 viste di default puntate su un host cloud reale (:38). `logoutUnifi()` (:60-63) fa POST a `/api/auth/logout` con credenziali sulla stessa TLS non validata. | Eliminare lo switch; `app.on('certificate-error', ...)` che accetta solo gli hostname presenti in `config.viste`, con `callback(false)` **loggato** negli altri casi. Flag `ignoraErroriCertificato` **obbligatorio** (non opzionale) e con default **true**, così nessuna installazione esistente cambia comportamento all'aggiornamento; invertire il default solo dopo un sito pilota. Ricalcolare la allowlist dopo Salva (:199) o un controller appena aggiunto viene rifiutato fino al riavvio; tollerare lo stesso controller raggiunto per IP in una vista e per hostname in un'altra. **Scartare** il pinning TOFU del fingerprint: blocca l'intera flotta il giorno in cui UniFi rigenera il certificato in un aggiornamento firmware. | Medio |
| `deny-device-permissions` | Nessun `setPermissionRequestHandler` / `setPermissionCheckHandler` / `setDevicePermissionHandler` sulla partition `persist:unifi` (:22, usata a :208). Senza handler Electron **approva** per default, senza prompt, e i grant persistono con la partition. Su un PC a muro con webcam e microfono è un canale silenzioso. | Tre righe su `getUnifiSession()` (helper già esistente a :49): `const ALLOW = new Set(['fullscreen']); s.setPermissionRequestHandler((wc,p,cb)=>cb(ALLOW.has(p))); s.setPermissionCheckHandler((wc,p)=>ALLOW.has(p)); s.setDevicePermissionHandler(()=>false);`. Tenere `fullscreen` in **entrambi** gli handler (il check è quello che regge il pulsante di espansione di Protect). Mettere `media` dietro un flag se un sito usa il talk bidirezionale, e loggare i deny: un permesso negato è invisibile e arriva come "il pulsante non fa niente". | Basso |
| `ipc-authorization-gate` (parte utile) | I 7 handler privilegiati (:87, :92, :103, :110, :135, :193, :199) sono registrati a livello di modulo e **nessuno** legge `event.sender`. Peggio: :131-132 `ipcMain.removeAllListeners('pass-ok'); ipcMain.once('pass-ok', callback);` con re-emit a :135 `ipcMain.emit('pass-ok')` rende `pass-ok` contemporaneamente segnale interno **e** canale IPC inviabile da un renderer — la finestra password, che è pre-autenticazione e ha `nodeIntegration:true`, può inviarlo e aprire le impostazioni senza password. | ~10 righe: eliminare il canale `pass-ok` e far chiamare la callback pendente direttamente dall'handler `check-pass-val` (`const cb = pendingCb; pendingCb = null; event.reply('pass-result', true); cb && cb();`), più un contatore di fallimenti con ritardo crescente e **cooldown, non blocco definitivo** (un blocco "fino al riavvio" su un kiosk che non si riavvia mai chiude fuori l'installatore). Aggiungere `if (BrowserWindow.fromWebContents(event.sender) !== settingsWin) return;` **solo** quando esiste il preload: oggi la finestra Protect non ha alcun accesso a `ipcRenderer`. **Scartare** namespacing dei canali, conversione totale a `handle` e modulo di costanti condiviso: churn per 7 canali e un renderer. | Medio |
| `electron-fuses-hardening` | Build Electron stock: `RunAsNode` attivo, quindi `set ELECTRON_RUN_AS_NODE=1 && "UniFi Protect Monitor.exe" -e "..."` è esecuzione arbitraria usando il binario firmato e allowlistato dell'app; `NODE_OPTIONS=--require evil.js` inietta nel main process. `package.json` non ha alcun blocco fuses. | `electron-builder ^24.13.3` (package.json:14) supporta `electronFuses` dalla 24.8: solo config, nessun codice. Attivare **le tre** che chiudono la via "usa il mio exe come interprete Node": `runAsNode:false`, `enableNodeOptionsEnvironmentVariable:false`, `enableNodeCliInspectArguments:false`. **Rimandare** `enableEmbeddedAsarIntegrityValidation`, `onlyLoadAppFromAsar` e `enableCookieEncryption`: le prime due su electron-builder 24/Windows possono produrre un exe che non parte senza messaggio (scoperto presso il cliente, su installazione per-machine); la terza ri-chiave il cookie store e invalida la sessione Protect di **ogni** kiosk nello stesso giorno. Verificare con `npx @electron/fuses read --app dist/win-unpacked/"UniFi Protect Monitor.exe"` **e** eseguendo davvero l'installer. | Medio |
| `no-bridge-on-untrusted-origin` | Invariante oggi corretto ma non documentato: :208 `nodeIntegration:false, contextIsolation:true, partition: UNIFI_PARTITION` e **nessun** preload — l'origine remota Protect non ha alcun accesso IPC. Tre idee in circolazione (overlay di stato, auto-login, toast della vista attiva) vogliono tutte attaccarci qualcosa. | Un commento di tre righe sopra la BrowserWindow a :208 che enuncia la regola. Il riferimento dimostra il fallimento: `ref-upv/src/js/preload.js:99 openExternal: (url) => ipc.send('openExternal', url)` raggiunge `ref-upv/src/main/ipc.js:48 shell.openExternal(url)` senza allowlist né controllo del mittente — la pagina remota può lanciare schemi URL arbitrari sull'host. | Basso |

---

## 3. Affidabilità 24/7

| id | problema | fix | impatto |
|---|---|---|---|
| `no-load-failure-retry` / `did-fail-load-recovery` | `grep did-fail-load / did-finish-load` = 0, e :83 `win.loadURL(v.url, {userAgent: CHROME_USER_AGENT})` non ha `.catch()`. NVR non raggiungibile all'avvio (il PC parte prima dell'UDM, riavvio firmware, switch PoE) = unhandled rejection + pagina di errore Chromium in inglese, per sempre: nessun retry. Aggravante: con `logoutOnStart:true` il primo load avviene subito dopo `clearStorageData/clearAuthCache/clearCache` (:66-68), quindi il fallimento a freddo è il caso **comune**. | ~15 righe, il grosso del plumbing esiste già. `win.webContents.on('did-fail-load', (_e,code,_d,url,isMainFrame) => { if (code === -3 \|\| !isMainFrame \|\| isShowingErrorPage) return; ... })`. Le tre guardie sono portanti: `-3` (ERR_ABORTED) lo genera l'app stessa a ogni Ctrl+N che aborta il load precedente; `!isMainFrame` evita i sotto-risorse; il latch `isShowingErrorPage`, azzerato su `did-finish-load`, evita la ricorsione se fallisce la pagina di errore. Caricare **`wallpaper.html`** — già in `build.files` (package.json:23), già di brand (#1c2b39, wordmark iota/tau), oggi peso morto mai referenziato. Retry su `loadVista(vistaAttiva)` con backoff 5/10/30 s. Il timer va **cancellato in testa a `loadVista()` (:79)** e in `beginShutdown()` (:72), altrimenti riporta la parete sulla vista fallita dopo che l'operatore ha già cambiato, o chiama `loadURL` su una finestra distrutta. `wallpaper.html` non ha script né preload: tenerla statica, il countdown lo guida il main. | Alto |
| `render-process-gone-recovery` | :8-10 attivano decode video accelerato e GPU rasterization su GPU integrate dove i reset del driver (TDR) sono di routine, ma non esiste alcun handler `render-process-gone`, `child-process-gone` o `unresponsive` (grep = 0): l'unico listener su `webContents` è `context-menu` (:215). Renderer morto = finestra grigia per sempre; pagina bloccata = ultimo fotogramma video congelato, e l'operatore guarda un'immagine ferma credendola live. | `win.webContents.on('render-process-gone', d => { log(...); recover(); })` con `recover()` = `win.reload()` ritardato di 3 s, escalation a `app.relaunch(); app.exit(0)` dopo 3 crash in 10 minuti. Il contatore e il check `win.isDestroyed()` sono portanti, non rifinitura: se è l'URL a uccidere il renderer, un handler senza gate trasforma una finestra morta in un loop di riavvii. Gestire `unresponsive`/`responsive` con timer a 60 s per **impedire** che compaia il dialog "pagina non risponde" di Electron su una parete video. `recover()` deve impostare `shuttingDown` o bypassare `beginShutdown()` (:229), altrimenti ogni riavvio da crash fa logout dal controller. `child-process-gone` solo log. | Medio |
| `prevent-display-sleep` | `powerSaveBlocker` non è importato (:1) e non compare nel file. Il piano bilanciato di Windows spegne il display dopo 10 minuti e un kiosk per definizione non ha input. Il wake-lock implicito di Chromium copre un `<video>` in riproduzione e visibile — non copre la vista "Registrazioni" (timeline, UI statica), la pagina di errore, né i momenti di buffering. | `powerSaveBlocker.start('prevent-display-sleep')` (implica già `prevent-app-suspension`) in `createWindows()`, `stop()` su `will-quit`. Condizionato a un nuovo `config.impedisciStandby` (default true) con checkbox accanto a FullScreen (:152), così un sito che vuole lo spegnimento notturno non deve toccare il piano energetico di Windows. `start()` in try/catch e id loggato: è l'unica API che una group policy può far fallire, e un fallimento silenzioso è indistinguibile dal bug non corretto. | Medio |
| `single-instance-lock` | `grep requestSingleInstanceLock` = 0; il bootstrap è la sola riga :228 `app.whenReady().then(createWindows)`. Un doppio click sul collegamento desktop (package.json:50) apre una seconda istanza sulla **stessa** partition `persist:unifi`, i cui `globalShortcut.register` (:224) falliscono in silenzio perché l'OS li ha già dati alla prima. Peggio ancora: con `logoutOnStart:true` la seconda istanza esegue `logoutUnifi()` (:206) che fa `clearStorageData/clearAuthCache/clearCache` sullo **stesso** profilo su disco e butta il kiosk vivo sulla pagina di login. | Richiedere il lock **sopra** la lettura del config a livello di modulo (:42), non solo prima di `whenReady`, o le due istanze si contendono comunque `viste_config.json`. L'istanza perdente deve chiamare **`app.exit(0)`, mai `app.quit()`**: `app.on('before-quit')` (:229) fa preventDefault e chiama `beginShutdown()` → `logoutUnifi()` sulla partition condivisa, cioè disconnetterebbe esattamente l'istanza che si voleva proteggere. Su `second-instance`: restore + show + focus. Scartare l'idea di pilotare la vista via argv: è un'altra feature travestita. | Alto |
| `remove-disable-http-cache-switch` | :7 `app.commandLine.appendSwitch('disable-http-cache')` sotto il commento fuorviante `// --- OTTIMIZZAZIONE CODEC MISTI ---` (:5), accanto agli switch realmente media-related. Ogni navigazione è un load completo via :83, quindi ogni Ctrl+N ri-scarica l'intero bundle SPA di Protect. Contraddice il pulsante "Svuota Cache" (:103-107), che senza cache HTTP è quasi un no-op. | Farne un `config.disabilitaCache` con default **true** (= comportamento odierno), poi invertirlo su un sito pilota e misurare il tempo di switch prima/dopo su hardware reale. Lo switch è già in parte ridondante: `logoutUnifi()` chiama `clearCache()` (:68) a ogni avvio con `logoutOnStart`. Aggiungere al pulsante `clearStorageData({ storages: ['cachestorage','serviceworkers'] })` — Protect registra un service worker che `clearCache()` non tocca, ed è la vera causa di "ho svuotato la cache e non è cambiato niente". Nota: reload e `isDestroyed()` a :105 sono **già** presenti; non riproporli. | Basso |

---

## 4. Funzionalità nuove ispirate a unifi-protect-viewer

| id | cosa manca | fix | impatto |
|---|---|---|---|
| `autostart-at-login` | `grep setLoginItemSettings` = 0 e il blocco `nsis` (package.json:45-55) crea solo collegamenti desktop/menu Start. Dopo un blackout o un riavvio da Windows Update la parete resta nera finché qualcuno non fa login e doppio click. Nessuno dei due riferimenti lo implementa. | Nessuna persistenza in `viste_config.json`: renderizzare la checkbox da `app.getLoginItemSettings().openAtLogin` (il registro è la fonte di verità, così la UI non può mentire) e scrivere con `if (app.isPackaged) app.setLoginItemSettings({ openAtLogin: v, path: process.execPath })` — la guardia `isPackaged` evita che `npm start` scriva una Run key verso l'eseguibile di sviluppo. Default **off**: attivarla d'ufficio su tutte le installazioni all'aggiornamento è un incidente per l'IT del cliente. Due avvertenze da dire nella UI: con `logoutOnStart:true` (:33) il riavvio automatico arriva sulla pagina di **login** Protect, quindi la coppia va gestita insieme; e su installazione per-machine la Run key va nell'HKCU del tecnico, non dell'utente kiosk. | Alto |
| `startup-view-selection` | :209 `loadVista(1)` è un indice letterale, e `loadVista` non consulta `attiva`: una vista 1 disattivata viene comunque caricata all'avvio. Su impianto fresco il kiosk parte su `https://unifi.ui.com/`. Nessun setting "vista di avvio" esiste. | ~6 righe: chiave `vistaAvvio`, un `<select>` nella riga a :152, un campo nel payload (:184/:199), `loadVista(config.vistaAvvio \|\| 1)` a :209. Retrocompatibile per costruzione: config senza la chiave → 1 = comportamento odierno. Il fallback **non** deve finire sulla pagina impostazioni (una modale con password su una parete non presidiata è peggio di una vista sbagliata): vista configurata → prima vista `attiva` → vista 1 → `wallpaper.html`. | Medio |
| `tray-icon-with-view-switcher` | `grep Tray` = 0; `Tray`/`nativeImage` non sono nell'import a :1. L'unico menu è dentro la pagina Protect (:215): irraggiungibile se la finestra è minimizzata, coperta o col renderer morto. E `❌ Chiudi` (:220) è un click **non confermato** immediatamente sotto l'elenco viste: mirare all'ultima vista e sbagliare di una riga chiude il kiosk — e con `logoutOnExit:true` invalida anche la sessione NVR. | **Prima cosa, tre righe e indipendente**: avvolgere `❌ Chiudi` (:220) in `dialog.showMessageBoxSync({type:'question', buttons:['Annulla','Chiudi'], defaultId:0, cancelId:0})`. Poi il Tray: estrarre il template già costruito a :216-222 in `buildViewMenuTemplate()` e alimentare **sia** il menu contestuale **sia** il tray (o divergono); `type:'radio'` + `checked: i === vistaAttiva` (la variabile esiste già a :14/:82); voci Mostra/Nascondi, Impostazioni (via `checkPassword`), Riavvia, Esci; `tray.on('double-click')`; `app.once('before-quit', destroyTray)` contro l'icona fantasma. Icona da `icona1.ico` — oggi spedita (package.json:27) e referenziata da nessuna parte. `Chiudi` del tray deve passare da `beginShutdown()` (:72), non da `app.quit()`. Il tray **integra** il menu contestuale, non lo sostituisce: molti kiosk nascondono la taskbar. | Medio |
| `active-view-indicator` | Nessun modo di sapere quale vista è a schermo: il titolo è fisso a :208 e comunque invisibile (`autoHideMenuBar` + fullscreen), e le voci di menu a :217-218 non hanno né `type` né `checked`. | Quasi tutto il plumbing è già fatto: `vistaAttiva` esiste (:14, aggiornato a :82) e `loadVista` è già l'unico imbuto. Restano ~5 righe: `type:'radio', checked: i === vistaAttiva` sulle voci a :217-218. **Scartare** il toast via `executeJavaScript` (violerebbe `no-bridge-on-untrusted-origin`) e il `setTitle`+`page-title-updated` (nessuno vede la barra del titolo su una parete fullscreen). | Basso |
| `view-rotation-timer` | Niente cicla le viste (`grep setInterval` = 0): un display non presidiato può mostrare una sola vista. Il requisito classico "Ingresso → Piazzale → Magazzino ogni 30 s" è irrealizzabile. Nemmeno il riferimento lo fa (`onSwitchNextProfile` apre un selettore manuale): è un vero differenziatore. | Campo `durata` (secondi, 0 = non avanzare) nell'oggetto per-vista (:38), reso come quarto `<input type="number" min="0" max="3600">` nella riga flex già esistente (:144-149). Catena di un solo `setTimeout` armata e **cancellata dentro `loadVista()` stessa**, così switch manuali, retry e logout la annullano gratis. Toggle come voce `type:'checkbox'` nel menu contestuale. Retrocompatibile: `durata` assente o 0 = comportamento odierno. **Trappola di perdita dati**: `save-all-data` (:199) fa `config.viste = d.vistas` — sostituzione integrale dell'oggetto costruito da `saveAll()` (:184), che raccoglie solo `nome/url/attiva`. Renderizzare l'input e dimenticare il collector cancella le durate di tutte e 10 le viste a ogni salvataggio. Prerequisito: `no-load-failure-retry`, o una rotazione che atterra su una vista irraggiungibile lascia la pagina di errore fino al tick successivo. | Medio |
| `cli-startup-overrides` | `grep process.argv` = 0; l'avvio è fisso (:208 `fullscreen: config.avvioFullScreen`, :209 `loadVista(1)`). Ogni decisione di deployment è nel config condiviso: due kiosk dello stesso sito non possono partire su viste diverse senza passare a mano su ognuno. E non esiste **nessun** punto di ripristino: password dimenticata = spiegare al telefono come cancellare `%APPDATA%\unifi-protect-monitor\viste_config.json`. | `parseCliArgs(argv)` che scansiona **tutto** argv (mai uno slice fisso: argv differisce di un elemento tra `npm start` e l'exe pacchettizzato), ignora i flag sconosciuti e valida i valori. Spedire per primi i due banali: `--vista <n>` e `--reset-config` (quest'ultimo è il pezzo di maggior valore). `--monitor <n>` e `--fullscreen` solo quando un sito a due schermi li chiede davvero — `--monitor` richiede l'helper di ordinamento display e il posizionamento prima di `setFullScreen`, cioè quasi tutto lo sforzo. Override **runtime-only**: mai riscritti nel config, così un collegamento non può corrompere lo stato condiviso. Non mettere `--reset-config` su un collegamento desktop: è un azzeramento password in un click. | Medio |
| `machine-default-config-seed` | :16 `configPath = path.join(app.getPath('userData'), 'viste_config.json')` è per-utente, ma l'installer è `perMachine: true` (package.json:48). Profilo ricreato, secondo account Windows o re-imaging = 10 segnaposto `https://unifi.ui.com/` e password di fabbrica, e qualcuno ridigita 10 URL. | Se il config utente non esiste, cercare `viste_config.default.json` accanto all'eseguibile (`path.dirname(app.getPath('exe'))`), passarlo dallo stesso riempimento di :46 e scriverlo una volta in userData — **mai** riscrivere il seed (un'installazione per-machine non ha permessi di scrittura in Program Files a runtime). Effetto collaterale utile: installer per-cliente senza modifiche al codice. Il seed va consegnato dall'installer (`extraFiles`), passo facile da dimenticare e invisibile fino al re-imaging: loggare il path del seed quando viene usato. Sequenziare dopo `scrypt-password-hash` e seminare l'hash, non la password, o si ricrea la password identica su tutta la flotta del cliente. | Basso |
| `per-view-zoom` | Nessun controllo di zoom (`grep setZoomFactor` = 0). Una griglia 3×3 al 100% su parete 4K è illeggibile da lontano; una vista a singola camera allo stesso zoom spreca metà pannello. | `zoom` nell'oggetto per-vista, applicato **su ogni `did-finish-load`** (il fattore si azzera a ogni navigazione, e Chromium lo traccia per origine — con 10 viste sullo stesso host NVR, applicarlo una volta sola le fa sanguinare l'una nell'altra in modo apparentemente casuale). Input numerico nella riga impostazioni con clamp 0.5-3.0 e coercizione a 1 sul non-numerico. Ctrl+Plus/Ctrl+Minus window-local per tarare in loco, con **debounce** sulla persistenza (scrivere `viste_config.json` a ogni pressione vanifica la write atomica). Prerequisito: il merge per-campo, altrimenti il nuovo campo non arriva mai ai config già su disco. | Basso |
| `onscreen-shortcut-help` | Nessun aiuto in-app: la pagina impostazioni (:140-189) non contiene testo di aiuto e F10 non è documentato da nessuna parte. Su un pannello con tastiera e senza mouse non c'è superficie di controllo scopribile. | **Non** iniettare nella pagina Protect (CSP del controller + re-render React = aiuto intermittente, che è peggio di nessun aiuto). Costruirlo come piccola BrowserWindow frameless `alwaysOnTop` che si chiude a qualsiasi tasto — lo splash a :202 è già esattamente quel pattern, quindi sono ~15 righe. Legare F1 window-local via `before-input-event`, **mai** `globalShortcut` (F1 è Help in ogni applicazione Windows). Valore basso: il menu contestuale a :217 stampa già `🎥 {nome} (Ctrl+{i})` per ogni vista attiva. | Basso |
| `local-shortcuts-not-global` | :224-225 registrano Ctrl+0..9 e F10 **a livello OS**, per tutta la vita del processo, a prescindere dal focus: il kiosk ruba Ctrl+1..9 a ogni altra applicazione della macchina (tab del browser, Notepad++, VLC) e F10 è il tasto menu in quasi tutto il software Windows. Il boolean di ritorno di `register()` è scartato in entrambi i casi. Il riferimento non usa `globalShortcut` affatto. | Versione più economica e **non breaking**: mantenere `globalShortcut` ma scoparlo sul focus — registrare in `app.on('browser-window-focus')`, `globalShortcut.unregisterAll()` in `browser-window-blur`. Quattro righe, zero cambiamenti mentre il kiosk è a fuoco, e Ctrl+1..9 torna agli altri programmi appena non lo è. Loggare un `register()` che ritorna false. La variante `before-input-event` è corretta ma ha un costo: gli hotkey muoiono mentre la finestra password o Configurazione ha il focus (sono BrowserWindow separate, :117/:139), cosa che oggi non accade. Su un PC dedicato al solo kiosk il guadagno è nullo: vale sulle workstation a uso misto. | Basso |

---

## 5. Build, packaging e manutenzione

| id | problema | fix | impatto |
|---|---|---|---|
| `git-repo-readme-changelog` | Nessun `.git` nella cartella progetto né sopra; nessun README, CHANGELOG, LICENSE. Il `.gitignore` esiste già con le quattro righe giuste (`node_modules/ dist/ out/ *.log`) — qualcuno voleva un repo e non l'ha mai creato. Il costo è già maturato: `project/main.js` (232 righe) è divergente da `project/_original_1.6.4/main.js` (156) senza alcuna traccia di cosa e perché, il versionamento si fa **per copia di cartella**, e nessuno può stabilire quale sintomo codec giustificasse gli switch a :6-10. Fatti operativi che vivono solo nella testa dell'autore: password di default (:28), F10 (:225), path del config (:16), che Restore rilancia l'app (:98). | `git init`, commit dell'albero come v1.6.4 **prima** di qualunque altra modifica di questa roadmap, così ogni fix è rivedibile come diff. README piatto con le quattro cose non ricavabili altrove: tabella `\| Tasto \| Azione \|` (Ctrl+1..9 viste, Ctrl+0 Registrazioni, F10 Impostazioni), `%APPDATA%\unifi-protect-monitor\viste_config.json`, come resettare una password dimenticata, `npm ci` → `npm run dist`. CHANGELOG Keep-a-Changelog, anche solo con "pre-1.6.4: nessuna storia registrata". **Scartare** la cartella `screenshots/` e il workflow CI tag-guard: manutenzione che un negozio da una persona non farà, e `build.files` è comunque una allowlist. ⚠️ Committare così com'è mette `Uat07Iot` (:28) in history per sempre: se il repo andrà mai oltre la macchina locale, risolvere `scrypt-password-hash` prima del primo commit. | Alto |
| `build-files-whitelist-hygiene` | `package.json:20-29` elenca 8 file. Verificato per hash: `wallpaper.png` è identico a `logo.png` (86ada2491a8566b11a89ccf9d7db233f) e `build/icon.ico` identico a `icona.ico` (ff36b085ead68a42281fceb9a3088057); `icona1.ico` (:27) non è referenziato in `main.js` (che usa solo `icona.ico` a :17). ~290 KB di payload morto in un'app da ~11 KB di codice. Rischio maggiore: essendo una allowlist **opt-in**, il primo nuovo file (settings.html, preload-settings.js) viene silenziosamente escluso dall'asar e l'exe fallisce a runtime, non in build. | Eliminare `wallpaper.png`, `build/icon.ico` e — solo se il tray non lo usa — `icona1.ico`; puntare `buildResources` su `icona.ico`. **Conservare `wallpaper.html`**: è la pagina di errore di `no-load-failure-retry`. Sostituire l'elenco con un glob **positivo** stretto (`["main.js","*.html","logo.png","icona.ico","package.json"]` + eventuale path src), **non** `["**/*", "!...."]`: la cartella contiene `_original_1.6.4/` e `build/`, e una deny-list che ne manca uno spedisce i vecchi sorgenti dentro l'asar. Farlo **prima** che atterri qualunque nuovo file sorgente. | Basso |
| `package-lock-and-manifest-metadata` | Nessun `package-lock.json`, mentre `"electron-builder": "^24.13.3"` (:14) ha il caret accanto a `"electron": "28.3.3"` (:13) fissato esatto — cioè la parte che fluttua è proprio il tooling di build. Due build dello stesso tag 1.6.4 producono installer diversi con lo stesso numero di versione. Mancano `license`, `private`; la descrizione (:4) è una riga di changelog ("Fix Codec Misti"), non un prodotto. | `npm install` per generare il lockfile e committarlo, build con `npm ci`, pin esatto di electron-builder **alla versione realmente in uso (24.13.3)** — non un salto a 26.x travestito da pin, che cambia NSIS e signing. Riscrivere la descrizione come prodotto. Aggiungere `license` e `private`. **Non** aggiungere script `test`/`release` che puntano a file inesistenti: uno script che fallisce all'invocazione è peggio di nessuno script. `engines` solo se verificato contro il Node effettivamente in uso. | Basso |
| `nsis-code-signing` | I blocchi `win` (:34-44) e `nsis` (:45-55) contengono solo chiavi cosmetiche: nessun `certificateFile`, `certificateSubjectName`, `signtoolOptions`, `sign`. Con `perMachine: true` (:48) ogni installazione mostra il prompt UAC giallo "Autore sconosciuto", più l'interstiziale SmartScreen a schermo intero — problema di credibilità quando si installa davanti al cliente. | Azure Trusted Signing (~10 $/mese, nessun HSM da gestire, reputazione SmartScreen inclusa) piuttosto che acquistare OV/EV. Configurare `signtoolOptions` con `publisherName`, `signingHashAlgorithms: ["sha256"]` e — la parte non ovvia e obbligatoria — `rfc3161TimeStampServer`: senza timestamp la firma smette di validare il giorno di scadenza del certificato e gli installer già consegnati iniziano a fallire. `signtool verify /pa /v` come passo di build, così un artefatto non firmato non può partire in un giorno in cui mancano le variabili d'ambiente. Bloccato da procurement, non da ingegneria: pianificarlo presto. Mantenere `perMachine: true`. | Medio |
| `electron-28-to-supported-major` | `"electron": "28.3.3"` (:13) = Chromium 120, fuori dalla finestra di supporto (ultime tre major) da metà 2024, mentre `"copyright": "Copyright © 2026 Iotatau"` (:18). Il riferimento è su 42.9.3. | **Sequenza obbligata**: prima `settings-window-data-over-ipc` (le Electron recenti restringono progressivamente le `data:` URL privilegiate, ed è esattamente come sono costruite entrambe le finestre a :130/:190), poi il bump — una major alla volta, su una macchina di scorta collegata a un controller reale. Due regressioni da **testare, non assumere**: (1) `partition: 'persist:unifi'` (:22, :208) fa ereditare il vecchio profilo Chromium — se il cookie di sessione Protect non sopravvive, ogni kiosk chiede il login alla console lo stesso giorno; (2) :10 `appendSwitch('disable-features', 'HevcAdapter,HardwareMediaKeyHandling')` imposta **una sola** chiave: una Electron più nuova che voglia la propria voce `disable-features` viene silenziosamente sovrascritta da questa chiamata, e il sintomo è proprio il bug codec misti che l'app esiste per risolvere. Soak di 24 h su un kiosk con camere H.264/H.265 miste prima del rollout. Tenere 28.3.3 per le build cliente finché il playback non è verificato. | Medio |
| `session-user-agent-pinning` | :25 dichiara `Chrome/110.0.0.0` mentre Electron 28 monta Chromium 120, e `grep setUserAgent` = 0: la stringa è passata per-navigazione. **Correzione a una tesi diffusa**: `loadURL(url,{userAgent})` chiama `SetUserAgentOverride` sul WebContents, quindi copre anche sotto-risorse e XHR — non c'è il "leak di Electron/28.3.3" spesso citato. Ciò che è reale è l'obsolescenza e la duplicazione. | Costruire la stringa da `process.versions.chrome` + `os.release()` e impostarla una volta con `getUnifiSession().setUserAgent(...)`, poi togliere l'opzione dall'unica call site (:83). Tre righe, e non può più invecchiare attraverso l'upgrade di Electron. ⚠️ Il commento a :24 dice che questa UA serve a far servire a Protect uno stream H.264-compatibile — cioè lo scopo stesso del prodotto. Verificare su controller reale con camere miste, e **non** rilasciarlo insieme all'upgrade di Electron, o non si saprà quale dei due ha causato la regressione. | Basso |
| `main-process-log-file` | `grep 'console\.' main.js` = 0 su tutte le 232 righe: zero osservabilità. L'unico errore ispezionato è scartato (`catch (e) {}` a :43) e :99 mostra una modale che su un kiosk non presidiato nessuno vede. | ~6 righe: `logLine()` che appende a `path.join(app.getPath('userData'),'monitor.log')` con rotazione a singolo archivio `.1` a 2 MB e catch che inghiotte tutto (il logger non deve mai far cadere l'app). Chiamarlo da ~8 punti: app ready + versione, esito del load config, ogni `loadVista`, `did-fail-load` con codice e URL, `render-process-gone`, logout, apertura impostazioni, fallimenti password, `callback(false)` dei certificati e permessi negati. È il prerequisito diagnostico di `no-load-failure-retry`, `render-process-gone-recovery`, `scoped-certificate-trust` e `deny-device-permissions`, che altrimenti si recuperano in silenzio. Rimandare il pulsante "Apri log" a dopo il preload: cablare `shell.openPath` nella pagina nodeIntegration attuale aggiunge solo un motivo per tenerla privilegiata. | Medio |
| `deep-merge-config-defaults` (frammento salvato) | :34 `Object.assign` è **shallow**: `config.viste` viene preso integralmente dal file, quindi nessun nuovo campo per-vista (`durata`, `zoom`) riceverà mai un default sui config già installati. Inoltre :46 aliasa l'oggetto vivo `defaultConfig.viste[i]` dentro `config`. Un oggetto vista presente ma con `nome` mancante renderizza letteralmente `value="undefined"` (:147). | Una riga, sostituendo :46: `config.viste[i] = { ...defaultConfig.viste[i], ...(config.viste[i] \|\| {}) }` — riempie i buchi **per campo**, elimina l'aliasing e diventa il prerequisito di ogni futuro campo per-vista. Aggiungere coercizione `attiva === true` e, in `loadVista` (:79-84), quattro righe che rifiutano un URL il cui `new URL(u).protocol` non sia http/https — mostrando la pagina di errore invece di chiamare `loadURL`. **Mai** sostituire un URL malformato con il default di slot: lo schermo sembrerebbe vivo puntando alla cosa sbagliata. *(Il crash da slot mancanti è già prevenuto da :46 + le guardie di `loadVista`: non riproporlo come motivazione.)* | Medio |
| `prettier-format-pass` | Righe misurate: :199 = 301 caratteri, :184 = 281, :208 = 272, :128 = 258. I diff saranno illeggibili proprio nella release in cui si toccano più file. | `npx prettier --write main.js` una volta, come commit a sé, con `.prettierrc.json` = `{"printWidth": 120, "singleQuote": true}` committato per registrare l'impostazione. Zero dipendenze installate (npx scarica su richiesta), nessun hook. **Scartare** husky + lint-staged + `prepare` + lo script di permessi del commit hook: cerimonia per una persona su un file. Farlo subito dopo `git init` e prima dei fix, o il primo diff serio è illeggibile. | Basso |

---

## 6. Proposte scartate e perché

| id | perché no |
|---|---|
| `auto-login-remember-me` | Contraddice frontalmente una scelta di prodotto già spedita e attiva per default: `logoutOnExit: true` (:32) e `logoutOnStart: true` (:33), con la checkbox a :160 che recita "Richiedi sempre le credenziali all'avvio (vale anche dopo un blackout)" e `logoutUnifi()` (:57-69) che fa POST di logout e ripulisce storage. Significherebbe salvare le credenziali NVR del cliente nello stesso `viste_config.json` in chiaro, su un PC in una hall. Se l'obiettivo è evitare la schermata di login dopo un riavvio, la leva è **una sola riga**: mettere `logoutOnStart` a false, e la sessione in `persist:unifi` viene riusata. |
| `kiosk-chrome-stripper-v4` + `legacy-v2-v3-handlers` + `protect-version-resolver` + `waituntil-dom-polling-core` + `grid-aspect-fit-watcher` + `dark-theme-activation` | Tutta la corsia "iniezione nel liveview". Il riferimento porta 1006 righe di `preload.js` e handler v2/v3/v4 per inseguire classi hash-ate di Protect attraverso i firmware, mantenute da un team; qui l'app **non inietta nulla** (`grep executeJavaScript` = 1 sola riga, :60, la fetch di logout) e non ha preload. Adottarlo significa un file preload (+ modifica a `build.files`), chirurgia DOM su classi che cambiano a ogni release Protect, e manutenzione di selettori su siti clienti diversi — con fallimento silenzioso su ogni firmware nuovo. Alternativa a costo zero: puntare gli URL delle viste alla rotta liveview/fullscreen di Protect e configurare il layout in Protect stesso. |
| `unexpected-url-snapback` + `per-vista-kiosk-flag` | Il primo riporta forzatamente la pagina all'URL configurato: renderebbe inutilizzabile la vista 0 "Registrazioni", che è una timeline dentro cui l'operatore **deve** navigare. Il secondo esiste solo per ritagliare quell'eccezione, cioè è un flag per una feature scartata. Il bisogno legittimo sotto (la pagina non deve diventare una via d'uscita dal kiosk) è coperto da `block-popup-windows` nel main process, senza preload e senza combattere l'operatore. |
| `electron-updater-fleet-rollout` | La conversione a framework più pesante della lista: una dipendenza runtime dentro l'asar in un'app che oggi ne ha **zero**, un host di aggiornamento da mantenere con TLS valido a tempo indeterminato, il code signing come prerequisito duro, e installazione automatica senza rollback — per sostituire una chiavetta in un negozio che su quei siti ci va comunque. Versione economica del 90% del valore: la pagina impostazioni mostra già `v${v}` (:180); un pulsante che apre una pagina statica di versioni via `shell.openExternal`. |
| `remove-dead-gpu-switches` | L'analisi è probabilmente corretta (`HevcAdapter` non è una feature Chromium registrata; il decode hardware è già attivo di default), ma "probabilmente inerte" non è una ragione per toccare il percorso codec di un'app la cui descrizione in package.json è letteralmente "Fix Codec Misti". Se davvero non fanno nulla, cancellarli non compra niente e rischia tutto — e non c'è git history che dica quale sintomo risolvessero. Fare invece l'unica cosa utile: riscrivere il commento fuorviante a :5 e aggiungere una vista `chrome://gpu` nascosta dalle impostazioni, così la prossima domanda sui codec si risponde con dati. |
| `useragent-from-runtime-chromium` (metà "quattro call site") | Premessa obsoleta: oggi c'è **una** sola call site con `{ userAgent: ... }`, dentro `loadVista` a :83. Il refactoring proposto è già avvenuto. Resta solo la questione della stringa, trattata in §5. |
| `guard-destroyed-window-navigations`, `centralized-vista-navigation`, `config-deep-merge-defaults`, `config-shallow-merge-crashes-startup`, `viste-config-schema-validation` | Già implementate o refutate dal file reale: `loadVista()` (:79-84) **è** già l'helper centralizzato con le guardie `!v || !v.url || !win || win.isDestroyed()`, usato da tutte le call site (:209, :217-219, :224); :105 guarda già `win && !win.isDestroyed()`; e :45-46 riempie ogni slot mancante con un commento esplicito sui config di versioni precedenti — quindi i TypeError previsti non possono verificarsi. Citavano tutte righe di `_original_1.6.4/main.js`. Residui salvati: il merge per-campo (§5) e l'`attiva` negli shortcut (§1). |
| `kiosk-window-recreate-on-close` | Combatterebbe la sequenza di shutdown appena aggiunta: `win.on('close')` (:213) e `before-quit` (:229) passano da `beginShutdown()` (:72-77) che distrugge la finestra e chiama `app.quit()`; un handler di ricreazione farebbe correre il flag `shuttingDown` contro un nuovo `createWindows()` — che **non è re-entrante** (crea un altro splash alwaysOnTop a :202 e ri-registra gli stessi globalShortcut che ora falliscono in silenzio). Non aiuta comunque contro il crash del main process. Le vere risposte sono la Startup folder/Task Scheduler con restart-on-failure e la conferma su `❌ Chiudi`. |
| `split-main-into-modules` / `split-main-into-src-main-modules` | 232 righe leggibili in cinque minuti da una persona sono l'asset di manutenibilità principale dell'app; sette moduli più una modifica alla whitelist `build.files` scambiano questo con una struttura che qui non serve, invalidano i riferimenti di riga di ogni altra proposta, e non producono alcun guadagno comportamentale. Il difetto reale che citavano (divergenza menu/shortcut sul flag `attiva`) è una riga. Se mai si estrae qualcosa: `settings.html` (che serve comunque) e un `config.js` testabile. |
| `contract-tests-with-mock-electron` / `node-test-harness` / `extract-testable-modules-node-test` | Tre varianti della stessa cosa, e quattro dei casi di test citati mirano a codice **inesistente** (parsing CLI, predicato del refresh giornaliero, rotazione del logger). Versione minima difendibile: estrarre il solo normalizzatore di config + `verifyPassword` in `config.js`, `"test": "node --test"` (integrato, nessuna dipendenza, nessun runner, nessun mock `Module._load`) e tre asserzioni — riempimento per campo, rifiuto di URL non-http, migrazione password una sola volta. |
| `github-actions-tag-release` / `release-script-version-tag` | Il progetto non è un repo git, non ha lockfile e non ha script `test`: la pipeline proposta fallirebbe ai primi tre passi. La CI protegge un team dagli altri membri; qui non c'è un secondo sviluppatore. Il valore reale (installer mal etichettato) è già coperto: `artifactName` (package.json:46) è `UniFi_Monitor_${version}.${ext}` e segue automaticamente il bump, e `npm version patch` + `git push --follow-tags` fa bump, commit e tag in un comando senza alcun nuovo file. |
| `portable-build-target` | Il bisogno sotto (deployment preconfigurato) è già coperto da `machine-default-config-seed` più i pulsanti Backup/Restore esistenti (:87-101), con un solo artefatto invece di due. Rivalutare solo se un cliente chiede davvero un deployment USB senza installazione. |
| `safestorage-credentials` | Presuppone un campo credenziali per-vista che l'app non ha e non deve acquisire. La metà reale (smettere di salvare `passwordApp` in forma reversibile) è coperta meglio da `scrypt-password-hash`: un hash non va mai decifrato, mentre `safeStorage` è DPAPI e romperebbe la portabilità dei file di Backup tra macchine. |
| `inline-strings-table` / `views-array-model-with-hotkeys` / `config-schema-version-and-migrations` / `preserve-unknown-view-fields-on-save` | Churn su un prodotto monolocale e mono-scopo. In particolare `views-array-model-with-hotkeys` riscriverebbe la forma su disco di `viste_config.json` (da oggetto `'0'..'9'` a array con UUID) su kiosk già installati, invalidando ogni file di Backup mai esportato da qualsiasi cliente — in cambio di riordino e numero variabile di viste, mentre i **dieci slot a cifra fissa sono il prodotto** (la UI mostra "Ctrl+N" per riga, il menu pure, e il modello mentale dell'operatore è la tastiera). Di `config-schema-version` è difendibile solo scrivere `schemaVersion: 1` ora, per avere su cosa ramificare in futuro. |
| `flush-session-storage-on-quit` | Premessa invertita: l'app **distrugge deliberatamente** la sessione (`logoutOnExit`/`logoutOnStart` a :32-33). Salvare lo storage "perché il login sopravviva al riavvio" va contro il design. Il difetto che sfiorava è reale ma diverso e va corretto lì: `import-config` a :98 fa `app.relaunch(); app.exit()` saltando del tutto il path di shutdown. |
| Tutti i duplicati | Un solo item per difetto. Canonici scelti: `implement-or-remove-auto-reboot` (contro `dead-auto-reboot-control`, `ghost-refresh-schedule`, `dead-autoreboot-scheduler`, `retire-dead-reboot-setting`, `dead-auto-reboot-setting`, `auto-reboot-scheduler-missing`); `scrypt-password-hash` (contro `hash-app-password`, `password-hash-scrypt`, `hashed-settings-password`); `no-load-failure-retry` (contro `did-fail-load-offline-page`, `reconnect-retry-overlay`, `did-fail-load-recovery`, `kiosk-resilience-handlers`); `local-shortcuts-not-global` (contro `window-local-shortcuts`, `global-shortcuts-hijack-machine`, `scoped-shortcuts-not-global`, `window-scoped-shortcuts`, `honor-attiva-in-shortcuts`, `shortcut-guard-inactive-views`); `build-files-whitelist-hygiene` (contro `dead-assets-wallpaper-icona1`, `prune-duplicate-packaged-assets`); `remove-disable-http-cache-switch` (contro `http-cache-switch-cost`, `http-cache-for-24-7-kiosk`, `http-cache-vs-reload-cycle`); `settings-window-fits-workarea` (contro `settings-window-fits-screen`, che usa `getPrimaryDisplay` — sbagliato quando la parete è il monitor secondario). |

---

## 7. Piano in 3 release

### v1.7.0 — "quick wins"
**Obiettivo: eliminare i fallimenti che oggi richiedono di andare fisicamente sul posto, senza toccare la struttura del file.**

Prerequisiti da fare per primi, nell'ordine: `git-repo-readme-changelog` (commit dell'albero attuale) → `prettier-format-pass` → `build-files-whitelist-hygiene`.

| id | nota |
|---|---|
| `git-repo-readme-changelog` | Storia + README con hotkey, path config, reset password. Prima di tutto il resto. |
| `prettier-format-pass` | Un commit a sé, prima dei fix, o i diff sono illeggibili. |
| `escape-config-in-settings-html` | 4 righe di `esc()` su ogni `${}` di entrambe le pagine, inclusa `oraReboot` (:153). Chiude oggi il path injection→RCE. |
| `strip-default-menu-devtools` | `Menu.setApplicationMenu(null)` + voce `🔄 Ricarica` nel menu contestuale, stesso commit. |
| `corrupt-config-quarantine` + `atomic-config-write` | Insieme: uno smette di produrre il file troncato, l'altro smette di ingoiarlo in silenzio. |
| `no-load-failure-retry` | `did-fail-load` + `wallpaper.html` + retry con backoff. Il singolo item con il miglior rapporto valore/righe. |
| `single-instance-lock` | Lock sopra la lettura del config; istanza perdente con `app.exit(0)`. |
| `main-process-log-file` | 6 righe; senza questo i tre item sopra si recuperano in silenzio e nessun intervento è diagnosticabile. |
| `implement-or-remove-auto-reboot` | Ticker 60 s + checkbox mancante. Default `autoReboot: false`. |
| `shortcut-ignores-attiva` | Guardia nel loop di registrazione (:224), non in `loadVista`. |
| `splash-fixed-2s-timeout` | Latch + `isDestroyed()` + `backgroundColor:'#1c2b39'`. |
| `modal-prompt-auto-dismiss` + `password-prompt-keyboard` | Riferimenti a scope di modulo, guardia di re-entranza, Enter/Escape. |
| `settings-window-fits-workarea` | Clamp su `workAreaSize` **e** `overflow-y:auto` sul body — le due metà insieme. |
| `deny-device-permissions` | 3 righe su `getUnifiSession()`. |
| `prevent-display-sleep` | `powerSaveBlocker` + flag `impedisciStandby`. |
| `block-popup-windows` | `setWindowOpenHandler` con `origin` (non `startsWith`), deny + log. |
| `tray-icon-with-view-switcher` (solo conferma Chiudi) | Le 3 righe di `showMessageBoxSync` su `❌ Chiudi` (:220). Il tray arriva in v1.8.0. |
| `deep-merge-config-defaults` (frammento) | Merge per-campo a :46 + validazione schema http/https in `loadVista`. |
| `package-lock-and-manifest-metadata` | Lockfile, pin esatto di electron-builder 24.13.3, descrizione di prodotto. |

### v1.8.0 — "refactor + sicurezza"
**Obiettivo: rimuovere la costruzione `data:` + nodeIntegration, mettere in sicurezza credenziali e TLS, e rendere il kiosk auto-riparante.**

| id | nota |
|---|---|
| `settings-window-data-over-ipc` | Il commit strutturale della release: `settings.html` + `password.html` + `preload-settings.js`, `contextIsolation:true`, bridge nominale, CSP, 7 canali riportati, `build.files` aggiornato **nello stesso commit**. |
| `scrypt-password-hash` | Entrambi i confronti (:135, :194) nello stesso commit; migrazione dal plaintext; password esclusa da export/import; reset documentato. |
| `validated-config-import` | Mai importare una credenziale, check di forma minimo, dialog di conferma. |
| `ipc-authorization-gate` (parte utile) | Eliminare il canale `pass-ok`, callback diretta, contatore con cooldown. Guardia sul sender ora che il preload esiste. |
| `scoped-certificate-trust` | Rimuovere lo switch globale; handler per hostname configurati; flag `ignoraErroriCertificato` default **true**; loggare ogni `callback(false)`. |
| `navigation-allowlist` (metà `will-navigate`) | Bloccare solo gli schemi non-http(s). Nessuna allowlist per hostname: romperebbe l'SSO. |
| `electron-fuses-hardening` | Solo `runAsNode`, `enableNodeOptionsEnvironmentVariable`, `enableNodeCliInspectArguments`. Verifica con `@electron/fuses read` **e** esecuzione dell'installer. |
| `render-process-gone-recovery` | Reload ritardato + escalation a 3 crash/10 min + soppressione del dialog "non risponde". Bypassare `beginShutdown()`. |
| `remove-disable-http-cache-switch` | Come flag `disabilitaCache` default true; misurare su un sito pilota; aggiungere `serviceworkers` alla Svuota Cache. |
| `autostart-at-login` | Fonte di verità = registro; `app.isPackaged`; default off; avvertenza su `logoutOnStart`. |
| `startup-view-selection` | `vistaAvvio` + catena di fallback che non finisce mai su una modale. |
| `tray-icon-with-view-switcher` (completo) | `buildViewMenuTemplate()` condiviso, radio su `vistaAttiva`, `destroyTray` su before-quit. |
| `active-view-indicator` | 5 righe: `type:'radio', checked` sulle voci :217-218. |
| `local-shortcuts-not-global` | Variante focus-scoped (register su `browser-window-focus`, `unregisterAll` su blur): non breaking. |
| `nsis-code-signing` | Da avviare come procurement già in v1.7.0; la config atterra qui. `rfc3161TimeStampServer` obbligatorio. |
| `cli-startup-overrides` (parziale) | Solo `--vista <n>` e `--reset-config`; scansione dell'intero argv. |

### v2.0.0 — "feature parity"
**Obiettivo: superare il riferimento sul terreno che conta per un video wall, su una base Electron supportata.**

| id | nota |
|---|---|
| `electron-28-to-supported-major` | Solo **dopo** `settings-window-data-over-ipc`. Una major per volta, soak 24 h su camere miste reali, verifica sopravvivenza cookie di `persist:unifi` e della chiave `disable-features`. Nessun altro cambiamento media nella stessa release. |
| `view-rotation-timer` | Il differenziatore che il riferimento non ha. `durata` per vista, timer armato/cancellato dentro `loadVista`. Ricordare il collector in `saveAll()` (:184). |
| `session-user-agent-pinning` | UA da `process.versions.chrome`, `setUserAgent` una volta sola. **Release separata** dall'upgrade Electron per isolare eventuali regressioni codec. |
| `per-view-zoom` | Dopo il merge per-campo. Applicare su ogni `did-finish-load`; clamp 0.5-3.0; persistenza con debounce. |
| `machine-default-config-seed` | Seed accanto all'exe, mai riscritto, hash e non password, path loggato quando usato. |
| `cli-startup-overrides` (completo) | `--monitor` con `getSortedDisplays()` e posizionamento prima di `setFullScreen`, `--fullscreen`. |
| `onscreen-shortcut-help` | Finestra F1 frameless sul modello dello splash (:202), mai iniezione nella pagina Protect. F1 window-local. |
| `no-bridge-on-untrusted-origin` | Commento-regola sopra la BrowserWindow a :208, da mettere prima che qualcuno proponga di nuovo un overlay. |
| `config.js` + `node --test` | Normalizzatore + `verifyPassword` estratti, tre asserzioni, zero dipendenze. Unico frammento salvato dalla corsia test. |
